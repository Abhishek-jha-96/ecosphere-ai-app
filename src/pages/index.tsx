const Index = () => {
  return (
    <div className="flex min-h-screen items-center justify-center gradient-sky">
      <div className="text-center space-y-6">
        <h1 className="mb-4 text-4xl font-bold">🌍 Climate Study App</h1>
        <p className="text-xl text-muted-foreground">
          Your main app is now running with Firebase authentication!
        </p>
        <p className="text-sm text-muted-foreground">
          This page shouldn't normally be visible as the app handles routing automatically.
        </p>
      </div>
    </div>
  );
};

export default Index;